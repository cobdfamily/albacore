// Hook bootstrap. Loaded by index.html in any
// browser-capable shell. Phase C wires this to
// @cobd/core's real entry; for now it just
// stamps a hello so the wiring can be verified
// once the build pipeline lands.

console.log("@cobd/hook: ready");
