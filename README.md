# @cobd/currentmap

Maps accessibility attributes across platforms (iOS, Android, Web,
desktop) so the rest of the Tuna ecosystem can speak one vocabulary.

## Develop

```
npm install
npm run build     # tsc + ESM import fix + patch version bump
```

Output goes to `dist/index.js`.

## License

AGPL-3.0 — see `LICENSE`.
